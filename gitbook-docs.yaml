$schema: https://api.gitbook.com/openapi.yaml#/components/schemas/GitSyncSiteConfig
site:
  title: umbrella-work
  structure:
    - type: space
      key: docs
      title: umbrella-work
      path: docs
      default: true
      content:
        directory: ./gitbook
