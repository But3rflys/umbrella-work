# TierToken
Table to work with CTierToken.CTierToken extends CItem

TierToken.GetChoices(tier_token: CTierToken) -> integer[]
  Returns the choices (ability ids) of the CTierToken.
  tier_token: The TierToken to get the choices from.
