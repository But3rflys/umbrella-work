# Localizer
Table to work with cheat localizer.

Localizer.Get(str: string) -> string
  Returns localized string using current language.
Localizer.RegToken(str: string)
  Registers key (token) string to localizer.
