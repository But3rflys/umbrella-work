# Entities
Table to work with entity list.

Entities.GetAll() -> CEntity[]
  Get all entities on the map.
Entities.Contains(entity: CEntity) -> boolean
  Check entity in cheat list.
  entity: Entity to check.
